doctype html

block locals
  - var title = 'My WebSite';

html(lang="ja")
  head
    meta(charset="UTF-8")
    meta(main="viewport" content="width=device-width, initial-scale=1.0")
    title #{title}
  body
    .container
      // _menu.pugを読み込む
      include _menu.pug
      block content
      .footer
          | WebpackCourse
